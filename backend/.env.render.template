# Render Backend Environment Variables Template
# Copy these to your Render service environment variables

# Database
MONGODB_URI=your_mongodb_atlas_connection_string

# JWT Secrets (generate strong random strings)
JWT_SECRET=your_jwt_secret_at_least_32_characters_long
JWT_REFRESH_SECRET=your_jwt_refresh_secret_different_from_above_and_jwt_secret

# Environment
NODE_ENV=production
PORT=5000

# Frontend URLs (update with your actual Vercel URLs)
FRONTEND_URL=https://your-frontend-app.vercel.app
ADMIN_URL=https://your-admin-app.vercel.app

# Email Configuration (Nodemailer)
EMAIL_FROM=your-email@gmail.com
EMAIL_PASSWORD=your-app-password
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587

# SMS Configuration (optional - Twilio)
TWILIO_ACCOUNT_SID=your_twilio_account_sid
TWILIO_AUTH_TOKEN=your_twilio_auth_token
TWILIO_PHONE_NUMBER=your_twilio_phone_number

# Payment Configuration (Razorpay)
RAZORPAY_KEY_ID=your_razorpay_key_id
RAZORPAY_KEY_SECRET=your_razorpay_key_secret

# Important Notes for Render:
# 1. Do NOT include COOKIE_DOMAIN - cookies work better without domain restriction for cross-origin
# 2. Make sure FRONTEND_URL and ADMIN_URL exactly match your Vercel deployment URLs
# 3. Use strong, unique secrets for JWT_SECRET and JWT_REFRESH_SECRET
# 4. Set NODE_ENV=production for proper cookie settings
